<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Official Creative Child's Challenge Contestants Voting Platform</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background: #f8f9fa;
            min-height: 100vh;
            padding: 0;
        }

            body[style*="background-image"] {
                background-size: cover !important;
                background-position: center !important;
                background-repeat: no-repeat !important;
                background-attachment: fixed !important;
            }

        .top-bar {
            background: green;
            color: white;
            padding: 15px 0;
            text-align: center;
            font-size: 0.9em;
            letter-spacing: 0.5px;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 20px;
        }

        header {
            text-align: center;
            padding: 60px 0 40px;
            background: white;
            color: green;
            margin-bottom: 40px;
        }

            header h1 {
                font-size: 2.5em;
                color: #1a1a1a;
                font-weight: 700;
                margin-bottom: 15px;
                letter-spacing: -0.5px;
            }

            header p {
                font-size: 1.1em;
                color: #666;
                max-width: 600px;
                margin: 0 auto;
                line-height: 1.6;
            }

        .voting-info {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 20px;
            margin-bottom: 40px;
            border-radius: 4px;
        }

            .voting-info p {
                color: #856404;
                margin: 0;
                font-weight: 500;
            }

        .contestants-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 30px;
            margin-bottom: 60px;
        }

        .contestant-card {
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            border: 2px solid transparent;
            position: relative;
        }

            .contestant-card:hover {
                box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
                transform: translateY(-4px);
            }

            .contestant-card.selected {
                border: 2px solid green;
                box-shadow: 0 8px 24px rgba(37, 99, 235, 0.2);
            }

                .contestant-card.selected::before {
                    content: "✓ SELECTED";
                    position: absolute;
                    top: 15px;
                    right: 15px;
                    background: green;
                    color: white;
                    padding: 6px 12px;
                    border-radius: 4px;
                    font-size: 0.75em;
                    font-weight: 700;
                    z-index: 10;
                    letter-spacing: 0.5px;
                }

        .contestant-image {
            width: 100%;
            height: 280px;
            background-size: cover;
            background-position: center;
            position: relative;
            overflow: hidden;
        }

            .contestant-image::after {
                content: "";
                position: absolute;
                bottom: 0;
                left: 0;
                right: 0;
                height: 100px;
                background: linear-gradient(to top, rgba(0, 0, 0, 0.7), transparent);
            }

        .contestant-number {
            position: absolute;
            top: 15px;
            left: 15px;
            background: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 8px 14px;
            border-radius: 4px;
            font-weight: 700;
            font-size: 0.9em;
            z-index: 5;
        }

        .contestant-info {
            padding: 25px;
        }

            .contestant-info h2 {
                font-size: 1.5em;
                color: #1a1a1a;
                margin-bottom: 12px;
                font-weight: 700;
            }

        .info-row {
            display: flex;
            align-items: center;
            margin-bottom: 8px;
            color: #666;
            font-size: 0.9em;
        }

            .info-row strong {
                color: #1a1a1a;
                min-width: 80px;
                font-weight: 600;
            }

        .contestant-info .bio {
            color: #555;
            line-height: 1.7;
            margin-top: 15px;
            padding-top: 15px;
            border-top: 1px solid #e5e5e5;
            font-size: 0.95em;
        }

        .vote-btn {
            width: 100%;
            padding: 14px;
            background: green;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 1em;
            font-weight: 600;
            cursor: pointer;
            margin-top: 20px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

            .vote-btn:hover {
                background: #1d4ed8;
                transform: translateY(-2px);
            }

            .vote-btn:active {
                transform: translateY(0);
            }

        .submit-section {
            background: white;
            padding: 40px;
            text-align: center;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
        }

        .submit-vote-btn {
            padding: 18px 80px;
            background: green;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 1.2em;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 4px 12px rgba(22, 163, 74, 0.3);
        }

            .submit-vote-btn:hover {
                background: #15803d;
                transform: translateY(-2px);
                box-shadow: 0 6px 16px rgba(22, 163, 74, 0.4);
            }

            .submit-vote-btn:disabled {
                background: lightgreen;
                color: white;
                cursor: not-allowed;
                transform: none;
                box-shadow: none;
            }

        .message {
            margin-top: 25px;
            padding: 20px;
            border-radius: 6px;
            font-size: 1.1em;
            font-weight: 600;
            display: none;
        }

            .message.show {
                display: block;
            }

            .message.success {
                background: #d1fae5;
                color: #065f46;
                border: 1px solid #10b981;
            }

        footer {
            background: #1a1a1a;
            color: white;
            text-align: center;
            padding: 30px 0;
            margin-top: 60px;
        }

            footer p {
                margin: 5px 0;
                opacity: 0.8;
            }

        @@media (max-width: 768px) {
            header h1

        {
            font-size: 1.8em;
        }

        .contestants-grid {
            grid-template-columns: 1fr;
        }

        .submit-vote-btn {
            padding: 16px 40px;
            font-size: 1em;
        }

        }
    </style>
</head>
<body style="background-image:url('/assets/img/backgroundimage.jpg')">
    <div class="top-bar" style="background-color:transparent">OFFICIAL VOTING PLATFORM 2024</div>

    <header style="opacity:0.95">
        <div class="container">
            <img src="~/assets/img/logo.jpg" style="width:100px; height:100px; border-radius:50px"/>
            <h1>Creative Child's Challenge Contestants Voting</h1>
            <p style="color: green">
                Cast your vote for your favorite contestant. Review each profile
                carefully before making your selection.
            </p>
        </div>
    </header>

    <div class="container">

        @RenderBody()

    </div>

    <footer style="background-color: green; color: white">
        <div class="container">
            <p>&copy; @DateTime.Now.Year Official Voting Platform. All rights reserved.</p>
            <p>For assistance, contact support@addaconsults.com</p>
        </div>
    </footer>

    @RenderSection("Scripts", false);
</body>
</html>