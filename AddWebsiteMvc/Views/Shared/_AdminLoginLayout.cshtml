<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Login - Voting Platform</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background: #f8f9fa;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .top-bar {
            background: green;
            color: white;
            padding: 15px 0;
            text-align: center;
            font-size: 0.9em;
            letter-spacing: 0.5px;
        }

        .login-container {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px 20px;
        }

        .login-box {
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            padding: 50px;
            width: 100%;
            max-width: 450px;
        }

        .login-header {
            text-align: center;
            margin-bottom: 40px;
        }

            .login-header h1 {
                font-size: 2em;
                color: #1a1a1a;
                font-weight: 700;
                margin-bottom: 10px;
            }

            .login-header p {
                color: #666;
                font-size: 1em;
            }

        .form-group {
            margin-bottom: 25px;
        }

            .form-group label {
                display: block;
                margin-bottom: 8px;
                color: #1a1a1a;
                font-weight: 600;
                font-size: 0.95em;
            }

            .form-group input {
                width: 100%;
                padding: 14px;
                border: 2px solid #e5e5e5;
                border-radius: 6px;
                font-size: 1em;
                transition: all 0.3s ease;
                font-family: inherit;
            }

                .form-group input:focus {
                    outline: none;
                    border-color: green;
                    box-shadow: 0 0 0 3px rgba(0, 128, 0, 0.1);
                }

                .form-group input::placeholder {
                    color: #999;
                }

        .remember-forgot {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .remember-me {
            display: flex;
            align-items: center;
            gap: 8px;
        }

            .remember-me input[type="checkbox"] {
                width: 18px;
                height: 18px;
                cursor: pointer;
                accent-color: green;
            }

            .remember-me label {
                color: #666;
                font-size: 0.9em;
                cursor: pointer;
            }

        .forgot-password {
            color: green;
            text-decoration: none;
            font-size: 0.9em;
            font-weight: 600;
            transition: opacity 0.3s ease;
        }

            .forgot-password:hover {
                opacity: 0.8;
            }

        .login-btn {
            width: 100%;
            padding: 16px;
            background: green;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 1.1em;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

            .login-btn:hover {
                background: #006400;
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(0, 128, 0, 0.3);
            }

            .login-btn:active {
                transform: translateY(0);
            }

            .login-btn:disabled {
                background: lightgreen;
                cursor: not-allowed;
                transform: none;
                box-shadow: none;
            }

        .divider {
            display: flex;
            align-items: center;
            text-align: center;
            margin: 30px 0;
            color: #999;
            font-size: 0.9em;
        }

            .divider::before,
            .divider::after {
                content: "";
                flex: 1;
                border-bottom: 1px solid #e5e5e5;
            }

            .divider span {
                padding: 0 15px;
            }

        .alternate-action {
            text-align: center;
            color: #666;
            font-size: 0.95em;
        }

            .alternate-action a {
                color: green;
                text-decoration: none;
                font-weight: 600;
            }

                .alternate-action a:hover {
                    text-decoration: underline;
                }

        .alert {
            padding: 15px;
            border-radius: 6px;
            margin-bottom: 25px;
            font-size: 0.95em;
            display: none;
        }

            .alert.show {
                display: block;
            }

            .alert.error {
                background: #fee;
                color: #c00;
                border: 1px solid #fcc;
            }

            .alert.success {
                background: #d1fae5;
                color: #065f46;
                border: 1px solid #10b981;
            }

        footer {
            background: green;
            color: white;
            text-align: center;
            padding: 30px 0;
            margin-top: auto;
        }

            footer p {
                margin: 5px 0;
                opacity: 0.9;
            }

        @@media (max-width: 768px) {
            .login-box

        {
            padding: 30px 20px;
        }

        .login-header h1 {
            font-size: 1.6em;
        }

        .remember-forgot {
            flex-direction: column;
            gap: 15px;
            align-items: flex-start;
        }

        }
    </style>
</head>
<body>
    <div class="top-bar">ADMIN LOGIN</div>

    @RenderBody()
   

    <footer>
        <div class="container">
            <p>&copy; @DateTime.Now.Year Official Voting Platform. All rights reserved.</p>
            <p>For assistance, contact support@addaconsults.com</p>
        </div>
    </footer>

    <script src="login.js"></script>
</body>
</html>
